let TOKEN = '';

export const setAuthData = (authData: any) => {};

export const tokenChecker = (authData: any) => {
  return true;
};

export const getAuthToken = (clearToken?: boolean) => {
  return '';
};

export const removeToken = () => {
  TOKEN = '';
};
